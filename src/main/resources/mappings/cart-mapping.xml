<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CartDAO">
	<!-- 사용자ID, 강좌명, 가격 넣기 -->
	<insert id="insert" parameterType="mycart" >
		INSERT INTO cart (num, email, vname, price)
		VALUES((SELECT NVL(max(num), 0)+1 FROM cart), #{email}, #{vname}, #{price})
	</insert>
	
	<!-- 장바구니 리스트, 코스 상세 설명 불러오기 -->
	<select id="getCartList" parameterType="mycart" resultType="mycart">
		SELECT *
		FROM cart
		WHERE email=#{email,jdbcType=VARCHAR}
	</select>
	
	<select id="listCount" resultType="java.lang.Integer">
		SELECT count(*)
		FROM cart
		WHERE email=#{email,jdbcType=VARCHAR}
	</select>
		
	<select id="totalPrice" resultType="java.lang.Integer">
		SELECT sum(price)
		FROM cart
		WHERE email=#{email,jdbcType=VARCHAR}
	</select>
	
</mapper>
